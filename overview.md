# CiteMap 功能规划概览（产品通视角）

> 日期：2026-07-31 · 角色：产品管理专家
> 目标：站在科研人员视角，梳理一个"好用、轻量"的本地论文管理工具还应满足什么，以及下一步该加什么。

## 一、先定准"用户要完成的 job"
科研人员用这类工具，真正要完成的不是"管理文件"，而是三件事：
1. **不漏掉**重要文献（发现 + 追踪）
2. **快速找回**读过的东西（收集 + 检索 + 组织）
3. **写论文时把知识串起来**（理解 + 综述 + 引用）

从这个 job-to-be-done 倒推，再对照现有 CiteMap：最酷的差异化（知识图谱、AI 标注、聊天）已做，但**每天最高频的"苦活"反而有缺口**。

## 二、生命周期覆盖度（现状）
| 环节 | 状态 | 说明 |
|---|---|---|
| 发现 | △ 部分 | 仅 arXiv 搜索/入库 |
| 收集 | △ 部分 | 缺 Zotero/BibTeX/DOI/PubMed 等多源导入 |
| 阅读·筛选 | ✗ 缺口 | 无阅读队列 / 阅读状态 |
| 理解·标注 | ✓ 已覆盖 | AI 标注 + Markdown 笔记 |
| 组织·关联 | △ 部分 | 图谱有，缺标签/智能合集 |
| 综述·写作 | △ 部分 | 笔记有，缺综述导出 |
| 引用 | ✗ 缺口 | 无 BibTeX 导出 |
| 追踪更新 | ✗ 缺口 | 无新文献提醒 |

## 三、推荐功能与优先级
**快速制胜（高价值·低成本，建议先做）**
- **引用导出（BibTeX/.bib）**：写论文刚需，解析已有元数据即可，几乎零成本。
- **Zotero / BibTeX / RIS 导入**：迁移门槛是 adoption 生死线，研究者已有现成库。
- **阅读队列 + 状态**（想读/在读/读完/归档 + 优先级）：感知价值高、实现极轻。
- **智能标签 / 智能合集（保存搜索）**：比知识图谱更日常的"分类"方式。

**重点投入（高价值·高成本，作为差异化）**
- **本地全文语义检索**：对 PDF 全文 + 笔记做本地向量索引，解决"那篇读过的论文在哪"。
- **基于本地库的 AI 阅读伴侣**：问答/总结/对比，且必须引用你库里的原文（vs 通用 ChatGPT 的差异化）。
- **文献综述自动生成**：从一组论文 + 笔记生成带引用的 Markdown 草稿。

**高成本·可选（谨慎评估）**
- 应用内 PDF 阅读器 + 高亮批注
- 浏览器剪辑插件（任意页面存文献）

## 四、轻量化护栏（Non-goals / 原则）
- 知识图谱降级为**二级入口**，不要做默认首页抢戏。
- **不重复造 Zotero**：做导入与集成，而非替代。
- AI 能力默认支持**本地模型**，成本可控、隐私可控。
- 坚持**单用户 / 本地优先**，不做多人协作、不做云锁定。
- 不做"大而全"，每个功能都先问：科研人员今天会不会真的用它？

## 五、下一步建议
1. 把"快速制胜"四项纳入下一个迭代（ROI 最高）。
2. 对"重点投入"三项先做技术可行性 + 成本验证（尤其本地向量检索与本地模型）。
3. 如需，我可进一步产出：单项功能的 PRD，或下季度 Now-Next-Later 路线图。
